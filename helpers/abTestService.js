/**
 * Retrieve A/B test performance metrics.
 * Replace with your real data-fetching logic (Prisma, external API, etc.).
 */
export async function getAbTestStats(testId) {
  // TODO: fetch real stats from your database or analytics service
  return {
    testId,
    impressions: 12345,
    clicks: 678,
    conversionRate: 0.056,
    users: 2345,
  };
}
